import { PipesPersoPipe } from './pipes-perso.pipe';

describe('PipesPersoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipesPersoPipe();
    expect(pipe).toBeTruthy();
  });
});
